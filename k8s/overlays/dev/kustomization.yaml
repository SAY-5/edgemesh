apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: edgemesh-dev
namePrefix: dev-

resources:
  - ../../base

commonLabels:
  app.kubernetes.io/instance: edgemesh-dev
  environment: dev
